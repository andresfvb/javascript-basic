// Selectores DOM

// const heading = document.querySelector('h2') // Selector por etiqueta
// const heading = document.querySelector('.heading') // Selector por clase
const heading = document.querySelector('#heading') // Selector por ID


const enlaces = document.querySelectorAll('.navegacion a') // Selector por ID


console.log(enlaces)